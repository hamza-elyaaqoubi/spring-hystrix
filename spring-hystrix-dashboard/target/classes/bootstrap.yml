---
spring:
  application:
    name: hystrix-dashboard

eureka:
  instance:
    nonSecurePort: ${server.port:9003}
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:9000}/eureka/
