---
spring:
  application:
    name: sample-eureka-client

eureka:
  instance:
    nonSecurePort: ${server.port:9001}
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:9000}/eureka/
